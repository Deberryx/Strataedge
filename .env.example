# Copy to .env.local and fill in real values. Never commit .env.local.

# Public site URL, used for canonical links and Stripe redirect URLs
NEXT_PUBLIC_SITE_URL=https://strataedge.netlify.app

# Resend (contact form + pricing inquiries)
RESEND_API_KEY=
CONTACT_TO_EMAIL=derekasa@strataedge.tech
CONTACT_FROM_EMAIL=StrataEdge <onboarding@resend.dev>

# Stripe (business website checkout) — use sk_test_... while testing
STRIPE_SECRET_KEY=
